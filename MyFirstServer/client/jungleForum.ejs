<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🍌JUNGLE FORUM🍌</title>
</head>
<body>
    <h1>🍌Message Monkey🍌</h1>
    <div>
        <form action="/jungleForum" method="POST" id="messageInput">
            <label for="email">email</label><br>
            <input type="email" id="email" name="email"><br>
            <label for="comment">message</label><br>
            <input type="submit" value="submit">
        </form>
        <textarea rows="4" cols="50" name="comment" id="comment" form="messageInput"></textarea>
    </div>

    <div>
        <% for( let i = (message.length - 1); i >= 0; i-- ) { %>
        <div>
            <h1><%- message[i].email %> </h1>
            <p><%- message[i].comment %> </p>
        </div>
        <% } %>
    </div>
</body>
</html>